{% extends 'base.html.twig' %}

{% block body %}
    <header>
        {% include 'home/nav.html.twig' %}
    </header>
    <div class="soclePage">
    <h1>Choisi ta lumiére...</h1>
    {% for collection in collections %}
            <div class="socle">
                <a href="{{ path('deco') }}"><img src="{{asset("build/pictures/") }}{{ collection }}" alt="led"></a>
            </div>
    {% endfor %}
    </div>
{% endblock %}